<h1>id:<%= @task.id %> のタスク編集</h1>

<%= form_for(@task) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>
  
  <%= f.submit '編集'  %>
<% end %>

<%= link_to '一覧に戻る',task_path %>